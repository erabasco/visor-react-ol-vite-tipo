name: Build and Deploy React App

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código fuente
        uses: actions/checkout@v2

      - name: Configurar token GH_TOKEN
        run: echo "GH_TOKEN=${{ secrets.GH_TOKEN }}" >> $GITHUB_ENV

      - uses: actions/setup-node@v3
        with:
          node-version: '18.16.0'  # Cambia la versión de Node.js según tus requisitos

      - name: Instalar dependencias
        run: npm install

      - name: Instalar Node.js y Vite
        run: |
          npm install -g vite
        if: success()

      - name: Construir la aplicación
        run: npm run build

      - name: Despliegue en servidor o plataforma
        uses: wlixcc/SFTP-Deploy-Action@v1.2.4
        with:
          delete_remote_files: true
          username: ${{ secrets.USER }}
          server: ${{ secrets.HOST }}
          password: ${{ secrets.PASSWORD }}
          local_path: './dist/*'  # Ruta local del directorio de archivos que deseas cargar
          remote_path: '/'  # Ruta remota en el servidor
          sftpArgs: '-o ConnectTimeout=5'
